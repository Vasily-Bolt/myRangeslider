let exp = require('chai').expect;
let path = require('path');
// let jQuery = require('jquery');
require(`../jquery.boltunovRangeslider/functions.ts`)

describe('plugin', () => {
    it('should return okay', () => {
        exp(true).to.be.true; //TODO
    });
});